syntax = "proto3";

package pb;

message Chunk {
  bytes data = 1;
}

message Done {}

service Raft {
  rpc SendSnapshot(stream Chunk) returns (Done);
}

message Snapshot {
  uint64 term = 1;
  uint64 index = 2;
}
